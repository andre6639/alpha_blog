<h1 align="center">
  All Categories
</h1>
<div class="center">
  <div class="inline">
    <%= will_paginate %>
  </div>
</div>
<% @categories.each do |category| %>
  <ul class="listing">
    <div class="center" >
          <%= link_to "#{category.name}", category_path(category) %>
    </div>
  </ul>
<% end %>
<div class="center">
  <div class="inline">
  <%= will_paginate %>
  </div>
</div>